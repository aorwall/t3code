interface:
  display_name: "Fork Upstream Merge"
  short_description: "Merge upstream and preserve T3 Code fork deltas"
  default_prompt: "Use $fork-upstream-merge to merge upstream or decide whether a file should take ours, theirs, or a converged resolution."
policy:
  allow_implicit_invocation: false
