/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Moatless Vibe Backend API
 * Sandbox orchestration and authentication for Moatless Vibe
 * OpenAPI spec version: 0.1.0
 */
import type { AgentType } from "./agentType.ts";
import type { TaskMessageTokenUsage } from "./taskMessageTokenUsage.ts";
import type { TaskSkillUsage } from "./taskSkillUsage.ts";
import type { TaskTokenTotals } from "./taskTokenTotals.ts";
import type { TaskToolCallStats } from "./taskToolCallStats.ts";

/**
 * Conversation statistics derived from persisted task messages.
 */
export interface TaskStatisticsResponse {
  agentType: AgentType;
  messageTokenUsage: TaskMessageTokenUsage[];
  /** @nullable */
  model?: string | null;
  skills: TaskSkillUsage[];
  taskId: string;
  tokenTotals: TaskTokenTotals;
  toolCalls: TaskToolCallStats[];
  /**
   * Whether this Task has a tool-call record at all.
   *
   * A Task that ran before tool calls were stored as their own rows has none,
   * and an empty `tool_calls` from that Task means "not recorded", not "made
   * no calls". The client has to be able to tell those apart — reporting zero
   * is a wrong answer, not a missing one.
   *
   * Defaults to `true` so a client reading an older response, or a response
   * from a deployment that predates the field, is not told every Task is
   * unrecorded.
   */
  toolCallsAvailable?: boolean;
}
